# Where the plop API lives.
#
# Leave it empty for local work: `npm run dev` sends /api to the uvicorn
# service on port 8000 (start it with `npm run api`).
#
# Set it on Vercel to the Render URL of the API, for example
# https://plop-api.onrender.com — with no slash at the end.
VITE_PLOP_API_URL=
